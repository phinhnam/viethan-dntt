# Sao chép file này thành .env rồi chỉnh giá trị theo môi trường của bạn.
# File .env KHÔNG được commit (đã có trong .gitignore).

HOST=127.0.0.1
PORT=5000
FLASK_DEBUG=1

# Tiền tố số phiếu cấp bởi API /api/next-number
DOC_NUMBER_PREFIX=DNTT
